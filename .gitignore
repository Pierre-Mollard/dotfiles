stow/docker/.docker/buildx
stow/docker/.docker/.token*

# external-wsl contains local symlink to windows for easier access
# windows is not looking in WSL dir space otherwize it would be slow
external-wsl/
